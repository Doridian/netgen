# BEGIN AUTOGENERATED
{%- for hostname, host in hosts.items() %}
address=/{{ hostname | format_host(domain) }}/{{ host.ip | format_ipaddr(network) }}
{%- if host.aliases %}
{%- for alias in host.aliases %}
address=/{{ alias | format_host(domain) }}/{{ host.ip | format_ipaddr(network) }}
{%- endfor %}
{%- endif %}
{%- endfor %}
# END AUTOGENERATED

address=/foxden.network/192.168.2.6
