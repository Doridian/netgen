# BEGIN AUTOGENERATED
{%- for hostname, host in hosts.items() %}
ptr-record={{ host.ip | format_ptrip(network) }}.{{ network | format_arpa() }},{{ hostname | format_host(domain) }}
#{{ host.ip | format_ptrip(network) }} IN PTR {{ hostname | format_host(domain) }}.
{%- endfor %}
# END AUTOGENERATED
